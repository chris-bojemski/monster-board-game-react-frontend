// import { combineReducers, createStore } from "redux";

// const defaultTeamOneState = [];

// const teamOneReducer = (state = defaultTeamOneState, action) => {
//   switch (action.type) {
//     case "LOAD_TEAM_FROM_API":
//       return action.payload;
//     default:
//       return state;
//   }
// };

// const rootReduce = combineReducers({
//   team1Roster: teamOneReducer
// });

// export const store = createStore(rootReduce);

// //store.dispatch({ type: "LOAD_TEAM_FROM_API", payload: [5, 6] });

// //debugger;
